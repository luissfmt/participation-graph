export interface ParticipationDTO {
    first_name: string,
    last_name: string,
    participation: number
};

export interface DeleteParticipationDTO {
    first_name: string,
    last_name: string,
};